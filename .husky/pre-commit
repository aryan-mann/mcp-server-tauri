# Rust checks (temporarily disabled due to missing system dependencies)
# cd packages/tauri-plugin-mcp-bridge
# cargo fmt --check
# cargo clippy -- -D warnings
# cd ../..

# JS/TS checks
npm run eslint

# Commitlint (temporarily disabled - will run in CI)
# if git rev-parse HEAD~1 >/dev/null 2>&1; then
#   npm run commitlint
# fi
